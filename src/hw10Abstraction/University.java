package hw10Abstraction;

public interface University extends College, Hospital {

	/*
	 * Question i) Total how many keywords are used for the inheritance in
	 * Interface, answer by Java comments? Can an interface inherit other
	 * Interfaces, or a regular class or abstract class by extends or implements
	 * keyword? How many inheritance is possible? Use the Interface -- "University"
	 * to answer my questions (University extends College, Hospital) (by multiple
	 * line comments).
	 */

//	Ans: In Interface one keyword is used - extends 
//	An interface can inherit other interfaces by extends keyword, but cannot inherit regular class or abstract class
//	An interface can inherit one or more interfaces

	
	
	
	
	
	
}
